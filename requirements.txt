# MergePilot Dependencies
# Python 3.11+ required

# ── Core ML ──────────────────────────────────────────────────────────────────
torch>=2.3.0
torchvision>=0.18.0
transformers>=4.45.0
datasets>=2.20.0
tokenizers>=0.19.0
accelerate>=0.33.0
peft>=0.12.0                    # LoRA fine-tuning
deepspeed>=0.15.0               # ZeRO-3 training
trl>=0.10.0                     # SFT, DPO, GRPO trainers
bitsandbytes>=0.43.0            # 4-bit quantization

# ── Code Analysis ────────────────────────────────────────────────────────────
pygments>=2.18.0                # Syntax highlighting and tokenization
gitpython>=3.1.0                # Git repository interaction
github3py>=4.0.0                # GitHub API client (PR discovery)
httpx>=0.27.0
aiohttp>=3.9.0

# ── Testing Infrastructure ────────────────────────────────────────────────────
pytest>=8.2.0                   # Test execution in sandbox
pytest-timeout>=2.3.0
black>=24.0.0                   # Code formatting validation
mypy>=1.10.0                    # Type checking

# ── Training Infrastructure ───────────────────────────────────────────────────
wandb>=0.17.0                   # Experiment tracking
huggingface-hub>=0.24.0         # Model upload/download
safetensors>=0.4.3

# ── vLLM Synthesis ────────────────────────────────────────────────────────────
vllm>=0.5.0                     # Fast LLM inference for synthesis
openai>=1.35.0                  # vLLM OpenAI-compatible client

# ── Data Processing ───────────────────────────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.0
pyarrow>=16.0.0

# ── Deployment ────────────────────────────────────────────────────────────────
fastapi>=0.112.0
uvicorn[standard]>=0.30.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.8.0
tqdm>=4.66.0
rich>=13.7.0
typer>=0.12.0
loguru>=0.7.0
datasketch>=1.6.5               # MinHash deduplication
anthropic>=0.40.0               # Claude API (synthesis backend)
aiofiles>=23.2.0                # Async file I/O for synthesis output
